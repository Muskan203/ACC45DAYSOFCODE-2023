#include <iostream>

int main() {
    int T;
    std::cin >> T;

    while (T--) {
        int X, Y;
        std::cin >> X >> Y;

        if (X > 10 * Y) {
            std::cout << "YES" << std::endl;
        } else {
            std::cout << "NO" << std::endl;
        }
    }

    return 0;
}
